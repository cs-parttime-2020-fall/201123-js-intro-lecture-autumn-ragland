# 20 11 23 JS intro Lecture

## Set Up
1. Create an html file with an `h1` rendering the assignment title
1. Create a JS file 
1. Link the JS file before the closing `body` tag using the `script` tag
1. Open the HTML file in the browser

## Console Output
1. Print the message `JS Linked!` in the browser using the `console.log` method : `console.log("JS Linked");`
1. Print the assignment title in the browser
1. Print your name in the browser

## Alerting in the Browser
1. Alert the message `JS intro Lecture` in the browser using the `alert` method : `alert("JS intro Lecture");`
1. Alert your name in the browser 

## User Input
1. Prompt the user to enter their name using the `prompt` method : `prompt("What's your name");`
1. Allow the user to confirm if they like JavaScript using the `confirm` method `confirm("Do you like JS"?);`

## Variables and Data types
1. Declare a variable called `userName` equal to your name
1. Declare a variable called `userAge` equal to your age
1. Declare a variable called `userIsInstructor` equal to false
1. Print the value of each variable to the browser
1. Redeclare the variable `userName` equal to your middle name
1. Print the value of the variable `userName` again
1. Print the value of the variable `userAge` plus 1
1. Print the inverse value of the variable `userIsInstructor` 
1. Update all variable declarations to use `let` instead of `var`
1. Redefine the variable `userName` instead of re-declaring it 
1. Prompt the user to enter their name and save the value in the variable `userNameInput`
1. Print the value of the variable `userNameInput`
1. Prompt the user to enter their age and save the value in the variable `userAgeInput`
1. Convert the type of the variable `userAgeInput`to an integer
1. Print the value of the variable `userAgeInput` plus 1
1. Allow the user to confirm if they are an instructor and save the value in the variable `userIsInstructorInput`
1. Print the value of the variable `userIsInstructorInput`

## String Formatting
```JS
`Plain text between back ticks with ${variables} between curly braces proceeded by a dollar sign`
```
1. Prompt the user to enter their name and save the value
1. Prompt the user to enter their age, convert the type to integer, and save the value
1. As the user to confirm if they are an instructor and save the value
1. Use a string template to print the message `Hey [NAME]! You are currently [AGE] years old. Next year you will be [AGE plus 1]. It is [INSTRUCTOR_BOOLEAN] that you are an instructor`
1. Use a string template to alert the above message